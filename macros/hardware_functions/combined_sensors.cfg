
[gcode_macro SET_FILAMENT_SENSOR]
rename_existing: SET_FILAMENT_SENSOR_BASE
gcode:

   SET_FILAMENT_SENSOR_BASE {rawparams}
   {% if params.SENSOR == "runout_sensor" and params.ENABLE is defined %}
      SET_FILAMENT_SENSOR_BASE SENSOR=motion_sensor ENABLE={params.ENABLE}
   {% endif %}